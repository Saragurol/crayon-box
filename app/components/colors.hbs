{{!-- <button>Add Color</button> --}}
<div class="colors">
    <div class='px-4'>
        <ul>
            {{#each @colors as |color|}}
            <li>
                {{!-- <button {{on 'click'(fn this.showPerson person)}}>{{person}}</button> --}}
                <a href='#'
                    class='block {{color.backgroundColor}} px-4 py-2 text-white hover:bg-green-600 hover:text-white border-white border-2  rounded-lg'>{{color.name}}</a>

            </li>
            {{/each}}
        </ul>
    </div>
</div>